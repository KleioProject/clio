mutation Login($email: String, 
               $password: String) 
    {
        login(email: $email, 
              password: $password) 
        {
            firstName
            id
            isActive
            isAdmin
            isSupervisor
            lastName
        }
}
